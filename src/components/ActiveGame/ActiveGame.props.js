/**
 * @typedef {Object} ActiveGameProps
 * @property {string} gameMode
 * @property {number} progressEpoch
 * @property {object} ctx  Adapter context passed to registry `props(ctx)`.
 */

export {};
