/**
 * @typedef {Object} ClassicEffectsProps
 * @property {boolean} ultraFlash
 * @property {boolean} showCombo
 * @property {number} combo
 * @property {{ ultra: string, fire: string }} comboLabels
 * @property {boolean} showSongName
 * @property {string} songName
 * @property {boolean} showIdle
 * @property {{ id: string|number, emoji: string } | null} keyFlash
 * @property {Array} trail
 * @property {Array} emojis
 * @property {Array} particles
 */

export {};
