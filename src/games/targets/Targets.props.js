/**
 * @typedef {object} TargetsProps
 * @property {string[]} activeEmojis
 * @property {(key: string, values?: Record<string, number | string>) => string} t
 * @property {(pattern: number[]) => void} vibrate
 */

export {};
